# Input variables
days_until_expiration = 5  # Example value
stock_level = 60  # Example value
product_type = "Perishable"  # Can be "Perishable" or "Non-Perishable"
    if 30%_discount_applied == (days_until_expiration <= 3) and (stock_level > 50)
    elif 20%_discount_applied == (days_until_expiration is 4:6) and (stock_level >50)

if 30% discount applied, print("30% discount applied") 